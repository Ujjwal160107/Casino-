node_modules
# Keep environment variables out of version control
.env
.env.test

# SSH keys never belong in the repo. ssh_key_ujjwal was committed in ab984c6
# and is retired, not recovered -- anything in history has to be rotated.
ssh_key*
*.pem
id_ed25519*
id_rsa*

# Runtime URI of the test Mongo memory-server (written by test/globalSetup.ts)
test/.mongo-uri

/src/generated/prisma
dist/
build/
coverage/
.DS_Store

# Superpowers SDD working artifacts (local process scratch)
.superpowers/
